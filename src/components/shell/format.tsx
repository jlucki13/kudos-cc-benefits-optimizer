/**
 * Money formatting for integer cents.
 *
 * Hoisted to src/lib/format.ts (W5); this file re-exports so existing
 * component imports keep working unchanged.
 */
export { formatCents, formatCentsSigned } from '@/lib/format';
